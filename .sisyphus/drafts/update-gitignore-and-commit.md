# Draft: Add dependency paths to .gitignore and commit to dev

- This draft outlines the steps to update .gitignore and commit to dev (or a feature branch).
- It preserves the plan for safe, reviewable changes via PRs when needed.
